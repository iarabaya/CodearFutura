import React from 'react';
import './customButton.css';


const CustomButton = ({text}) =>{
    return (
        <div>
            <button>{text}</button>
        </div>
    )
}
 export default CustomButton;